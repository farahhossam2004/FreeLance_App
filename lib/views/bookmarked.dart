import 'package:flutter/material.dart';

class BookmarkedJobsPage extends StatelessWidget {


  const BookmarkedJobsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('Saved Posts')],
    );
  }
}
